# Allow all crawlers
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://marichka.dev/sitemap.xml

# Crawl-delay (helps with server load)
Crawl-delay: 1

# Block specific user agents if needed (currently allowing all)
# User-agent: BadBot
# Disallow: /
